## Create Agent
```shell

curl -X "POST" "<%= config[:base_url] %>/agents" \\
     -H 'x-api-key: <%= config[:api_key] %>' \\
     -H 'tio-auth-token: <%= config[:api_token] %>' \\
     -H 'Content-Type: application/vnd.api+json' \\
     -d $'{
      "data":
      {
          "type": "agents",
          "attributes":
          {
              "first-name": "Cornelia",
              "last-name": "Gray",
              "email": "in@crunchy.bananas"
          },
          "relationships":
          {
              "workspace":
              {
                  "data":
                  {
                      "type": "workspaces",
                      "id": "<WORKSPACE_ID>"
                  }
              }
          }
      }
  }'
```

> The above command returns JSON structured like this:

```json
{
  "data": {
    "id":"3",
    "type":"employees",
    "attributes": {
      "first-name": "Cornelia",
      "last-name": "Gray",
      "email": "in@crunchy.bananas"
    }
  }
}
```

This endpoint creates a specific agent.

### HTTP Request

`POST <%= config[:base_url]%>/agents`

### BODY Parameters

Parameter | Description
--------- | -----------
WORKSPACE_ID | The WORKSPACE_ID that the agent is assigned
first-name | The first name of the agent
last-name | The last name of the agent
email | The email that will be used for authentication
 