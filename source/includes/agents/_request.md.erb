## Get a Specific Agent

```shell
curl "<%= config[:base_url] %>/agents/<ID>" \\
     -H 'x-api-key: <%= config[:api_key] %>' \\
     -H 'tio-auth-token: <%= config[:api_token] %>'
```

> The above command returns JSON structured like this:

```json
{
  "data": {
    "id": "4064",
    "type":"agents",
    "attributes": {
      "first-name": "Calvin",
      "last-name": "Broadus",
      "email": "snoop.dogg@example.com"
    }
  }
}
```

This endpoint retrieves a specific agent.

### HTTP Request

`GET <%= config[:base_url]%>/agents/<ID>`

### URL Parameters

Parameter | Description
--------- | -----------
ID | The ID of the agent to retrieve

## List Agents

```shell
curl "<%= config[:base_url] %>/agents" \\
     -H 'x-api-key: <%= config[:api_key] %>' \\
     -H 'tio-auth-token: <%= config[:api_token] %>'
```

> The above command returns JSON structured like this:

```json
{
  "data": [{
    "id": "738306350509981713",
    "type": "agents",
    "attributes": {
      "email": "guohawo@crunchy.bananas",
      "first-name": "Antonio",
      "last-name":"Hunter"
    }
  }],
  "meta": {
    "page": { "total":1 }
  }
}
```

This endpoint retrieves a list agent. Use the `?include=workspaces` to retrieve related workspaces.

### HTTP Request

`GET <%= config[:base_url]%>/agents`
