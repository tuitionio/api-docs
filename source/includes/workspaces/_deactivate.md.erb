## Deactivate a Specific Workspace

```shell
curl -X "DELETE" "<%= config[:base_url] %>/workspaces/" \\
     -H 'x-api-key: <%= config[:api_key] %>' \\
     -H 'tio-auth-token: <%= config[:api_token] %>' \\
```

This endpoint deactivates a specific workspace.

<aside class="success">
  Workspaces can not be deactivated if there are sub-workspaces
</aside>

### HTTP Request

`DELETE <%= config[:base_url]%>/workspaces/<ID>`

### URL Parameters

Parameter | Description
--------- | -----------
ID | The ID of the workspace to deactivate
