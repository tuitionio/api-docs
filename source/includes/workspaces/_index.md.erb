# Workspaces

## Get Workspaces

```shell
## Request Workspaces
curl "<%= config[:base_url] %>/workspaces?page%5Blimit%5D=2" \
     -H 'x-api-key: <%= config[:api_key] %>' \
     -H 'tio-auth-token: <%= config[:api_token] %>'
```

> The above command returns JSON structured like this:

```json
{
  "data":
    [{
      "id": "9877",
      "type": "workspaces",
      "attributes": {
        "name": "Tio",
        "description": "This is the parent workspace for all other workspaces",
        "path":[9877],
        ...
      },
    }, {
      "id": "9878",
      "type": "workspaces",
      "attributes": {
        "name":"Purple Admin Workspace",
        "description":"This is the purple workspace",
        "path":[9877,9878],
        ...
      }
    }],
    "meta": {
      "page": {
        "total":2
      }
    }
  }
```

`GET <%= config[:base_url] %>/workspaces?page%5Blimit%5D=2`

<aside class="success">
Remember — additional information is included in the response. See relations and parameters for more info. Using `include=agents` is a useful additional option.
</aside>

<%= partial "includes/workspaces/query_params.md" %>
<%= partial "includes/workspaces/request.md" %>
<%= partial "includes/workspaces/create.md" %>
<%= partial "includes/workspaces/update.md" %>
<%= partial "includes/workspaces/employees.md" %>
<%= partial "includes/workspaces/deactivate.md" %>



