## Get a Specific Employee

```shell
curl "<%= config[:base_url] %>/employees/<ID>" \\
     -H 'x-api-key: <%= config[:api_key] %>' \\
     -H 'tio-auth-token: <%= config[:api_token] %>'
```

> The above command returns JSON structured like this:

```json
{
  "data": {
    "id": "4064",
    "type":"employees",
    "attributes": {
      "first-name": "Anna",
      "last-name": "Riley",
      "email": "anna@test.com",
      "payroll-id": "123ABC",
      "is-active": true,
      "salary": 5000000,
      "contribution-amount": 15000,
      "custom-attributes": {
        "favorite-color": "yellow"
      }
    }
  }
}
```

This endpoint retrieves a specific employee.

### HTTP Request

`GET <%= config[:base_url]%>/employees/<ID>`

### URL Parameters

Parameter | Description
--------- | -----------
ID | The ID of the employee to retrieve

<%= partial "includes/employees/attributes.md.erb" %>
<%= partial "includes/employees/associations.md.erb" %>